package com.jjeanniard.plugins.enums;

/**
 * Définit les différents niveaux de logs personnalisés pour le plugin.
 * En regroupant ces types ici, on s'assure que tout le code utilise les mêmes constantes.
 */
public enum LogEnumType {
    INFO,
    WARNING,
    ERROR
}